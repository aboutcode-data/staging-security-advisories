advisory_id: GHSA-5339-hvwr-7582
datasource_id: github_osv_importer_v2/GHSA-5339-hvwr-7582
datasource_url: https://github.com/github/advisory-database/blob/main/advisories/github-reviewed/2026/03/GHSA-5339-hvwr-7582/GHSA-5339-hvwr-7582.json
aliases:
  - CVE-2026-31873
summary: |
  Unhead Vulnerable to Bypass of URI Scheme Sanitization in makeTagSafe via Case-Sensitivity
  The `link.href` check in `makeTagSafe` (safe.ts, line 68-71) uses `String.includes()`, which is case-sensitive:

  ```typescript
  if (key === 'href') {
    if (val.includes('javascript:') || val.includes('data:')) {
      return
    }
    next[key] = val
  }
  ```

  Browsers treat URI schemes case-insensitively. `DATA:text/css,...` is the same as `data:text/css,...` to the browser, but `'DATA:...'.includes('data:')` returns `false`.

  ### PoC

  ```javascript
  useHeadSafe({
    link: [{
      rel: 'stylesheet',
      href: 'DATA:text/css,body{display:none}'
    }]
  })
  ```

  SSR output:

  ```html
  <link rel="stylesheet" href="DATA:text/css,body{display:none}">
  ```

  The browser loads this as a CSS stylesheet. An attacker can inject arbitrary CSS for UI redressing or data exfiltration via CSS attribute selectors with background-image callbacks.

  Any case variation works: `DATA:`, `Data:`, `dAtA:`, `JAVASCRIPT:`, etc.

  ## Suggested fix

  ```typescript
  if (key === 'href') {
    const lower = val.toLowerCase()
    if (lower.includes('javascript:') || lower.includes('data:')) {
      return
    }
    next[key] = val
  }
  ```
impacted_packages:
  - purl: pkg:npm/unhead
    affected_versions: vers:npm/<=2.1.10
    fixed_versions: vers:npm/2.1.11
    fixed_in_commits: []
    introduced_in_commits: []
severities:
  - score: '0.0'
    scoring_system: cvssv3.1
    scoring_elements: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    published_at: None
    url: https://github.com/github/advisory-database/blob/main/advisories/github-reviewed/2026/03/GHSA-5339-hvwr-7582/GHSA-5339-hvwr-7582.json
  - score: LOW
    scoring_system: generic_textual
    scoring_elements:
    published_at: None
    url:
weaknesses:
  - CWE-79
references:
  - url: https://github.com/unjs/unhead
    reference_type:
    reference_id:
  - url: https://github.com/unjs/unhead/releases/tag/v2.1.11
    reference_type:
    reference_id:
  - url: https://nvd.nist.gov/vuln/detail/CVE-2026-31873
    reference_type:
    reference_id:
  - url: https://github.com/unjs/unhead/security/advisories/GHSA-5339-hvwr-7582
    reference_type: other
    reference_id: GHSA-5339-hvwr-7582
