advisory_id: npm/dompurify/GHSA-39q2-94rc-95cp
datasource_id: gitlab_importer_v2/npm/dompurify/GHSA-39q2-94rc-95cp
datasource_url: https://gitlab.com/gitlab-org/advisories-community/-/blob/main/npm/dompurify/GHSA-39q2-94rc-95cp.yml
aliases:
  - GHSA-39q2-94rc-95cp
summary: |
  DOMPurify's ADD_TAGS function form bypasses FORBID_TAGS due to short-circuit evaluation
  In `src/purify.ts:1117-1123`, `ADD_TAGS` as a function (via `EXTRA_ELEMENT_HANDLING.tagCheck`) bypasses `FORBID_TAGS` due to short-circuit evaluation.

  The condition:
  ```
  !(tagCheck(tagName)) && (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName])
  ```
  When `tagCheck(tagName)` returns `true`, the entire condition is `false` and the element is kept — `FORBID_TAGS[tagName]` is never evaluated.
impacted_packages:
  - purl: pkg:npm/dompurify
    affected_versions: vers:npm/<3.4.0
    fixed_versions: vers:npm/3.4.0
    fixed_in_commits: []
    introduced_in_commits: []
severities:
  - score: None
    scoring_system: cvssv3.1
    scoring_elements: CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:L/I:L/A:N
    published_at: None
    url: https://gitlab.com/gitlab-org/advisories-community/-/blob/main/npm/dompurify/GHSA-39q2-94rc-95cp.yml
weaknesses:
  - CWE-783
  - CWE-1035
  - CWE-937
references:
  - url: https://github.com/cure53/DOMPurify/security/advisories/GHSA-39q2-94rc-95cp
    reference_type:
    reference_id:
  - url: https://github.com/advisories/GHSA-39q2-94rc-95cp
    reference_type:
    reference_id: GHSA-39q2-94rc-95cp
